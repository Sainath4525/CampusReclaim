PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/campusreclaim
# Nodemailer SMTP Configuration (leave empty to log to console instead of sending real email)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM="CampusReclaim" <no-reply@campusreclaim.edu>
